# Test report — Entrevistou V2.2

Validações preparadas:
- TypeScript strict compilation
- Vite production build
- Spring context smoke test
- GET /api/dashboard integration test
- GET /api/learning-path integration test (6 módulos)

Observação: o build frontend foi executado no ambiente de geração antes da entrega. Os testes Maven estão incluídos no projeto e podem ser executados com `mvn test` em Java 21 + Maven 3.9+.
